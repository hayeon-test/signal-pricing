apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Values.appPrefix }}-test-connection"
  labels:
    app: {{ .Values.appPrefix }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Values.appPrefix }}:{{ .Values.service.port }}']
  restartPolicy: Never
